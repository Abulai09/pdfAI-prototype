# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# PyInstaller build output
build/
dist/
*.manifest

# Runtime data (SQLite journal, logs) — генерируется при запуске
journal.db
*.log

# Реальные PDF-выписки для тестов: никогда не коммитим
tests/fixtures/

# Сгенерированный граф знаний
graphify-out/

# Локальные настройки Claude Code
.claude/

# Артефакт Windows-редиректа (`> nul`)
nul

# Отчёт verify_any_file.py — пишется в корень при каждом прогоне батареи
verify_any_report.json

# Собранный дистрибутив (см. build/dist/ выше — тот же класс артефакта)
dist.zip

# Встроенный шаблон Kaspi ИП — настоящая выписка, в git не кладём
templates/
